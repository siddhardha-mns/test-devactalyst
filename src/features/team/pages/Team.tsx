import { Helmet } from 'react-helmet-async';
import Layout from '@/components/common/Layout';
import PageIntro from '@/components/common/PageIntro';
import { teamMembers } from '@/data/site-content';

export default function Team() { return <Layout><Helmet><title>Team — DevCatalyst</title></Helmet><PageIntro eyebrow="Team" title={<>Built by students who decided<br />to make room for more.</>}>DevCatalyst is shaped by students who organise, build, teach, and create opportunities for others to step into.</PageIntro><section className="px-5 py-16 sm:px-8 sm:py-24 lg:px-10"><div className="mx-auto max-w-7xl">{teamMembers.length === 0 ? <div className="border-y border-[#dedcd5] py-16"><p className="editorial-serif max-w-2xl text-4xl tracking-[-.045em]">The current organising team will be introduced here.</p><p className="mt-5 max-w-xl leading-7 text-[#5c616a]">Profiles will be published once each member’s role, bio, and photo are ready.</p></div> : <div className="grid gap-x-10 gap-y-12 sm:grid-cols-2 lg:grid-cols-3">{teamMembers.map((member) => <article key={member.id}><div className="aspect-[4/3] bg-[#e7eeff]">{member.portraitUrl && <img src={member.portraitUrl} alt={member.name} className="h-full w-full object-cover" />}</div><h2 className="editorial-serif mt-5 text-3xl tracking-[-.04em]">{member.name}</h2><p className="mt-1 text-sm font-extrabold uppercase tracking-[.1em] text-[#2455c9]">{member.role}</p><p className="mt-4 leading-7 text-[#5c616a]">{member.contribution}</p></article>)}</div>}<div className="mt-24 grid gap-8 border-t border-[#dedcd5] pt-12 sm:grid-cols-2"><p className="editorial-serif text-4xl tracking-[-.045em]">DevCatalyst is bigger than its organising team.</p><p className="max-w-xl leading-7 text-[#5c616a]">It grows through the students who attend, contribute, ask good questions, and keep building.</p></div></div></section></Layout>; }
